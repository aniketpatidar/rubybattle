<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing discussion</h1>
  <%= turbo_frame_tag dom_id(@discussion) do %>
    <%= render("form", discussion: @discussion) %>
    <%= link_to "Cancel", @discussion %>
  <% end %>
</div>
