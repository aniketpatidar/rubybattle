<!DOCTYPE html>
<html>
  <head>
    <title>Battle</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="" data-controller="slideover">
    <% if current_user %>
      <%= turbo_stream_from "online_users", channel: OnlineChannel %>
      <%= render "layouts/navbar" %>
    <% end %>
    <main class="container max-w-4xl mx-auto px-5 p-4" style="margin-top: 4rem;">
      <%= yield %>
    </main>
    <div id="flash-container">
      <% flash.each do |key, value| %>
        <%= render "shared/flash", level: key, content: value %>
      <% end %>
    </div>
    <%= render "shared/slideover" %>
  </body>
</html>
