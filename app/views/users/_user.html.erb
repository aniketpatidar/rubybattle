<a href="/users/<%= user.slug %>"
   id="<%= dom_id(user) %>"
   class="ck-card group block <%= local_assigns[:index] && index.even? ? 'rotate-[-0.5deg]' : 'rotate-[0.5deg]' %>"
   style="text-decoration: none;">
  <div class="flex items-center gap-4">
    <div class="flex h-14 w-14 shrink-0 items-center justify-center rounded-full border-4 border-black bg-ck-muted text-base font-black">
      <% if user.avatar.attached? %>
        <%= image_tag user.avatar, class: "h-full w-full rounded-full object-cover" %>
      <% else %>
        <%= user.initials %>
      <% end %>
    </div>
    <div class="min-w-0 flex-1">
      <div class="text-lg font-black uppercase leading-none text-ck-ink"><%= user.full_name %></div>
      <div class="ck-meta mt-1">@<%= user.slug %></div>
    </div>
    <div class="shrink-0">
      <%= render partial: 'users/status', locals: { status: @active_users.include?(user) ? 'Active' : 'Inactive' } %>
    </div>
  </div>
  <div class="ck-meta mt-3 pt-3" style="border-top: 2px solid var(--ck-line);">
    Joined <%= user.created_at.strftime("%b %d, %Y") %>
  </div>
</a>