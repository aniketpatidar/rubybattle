<div class="ck-page">
  <section class="mb-8 border-4 border-black bg-ck-secondary p-5 shadow-[8px_8px_0_0_#000] sm:p-8 lg:p-10">
    <div class="ck-sticker mb-5">Categories</div>
    <h1 class="ck-heading">Edit Category</h1>
  </section>

  <%= turbo_frame_tag dom_id(@category) do %>
    <div class="ck-card max-w-2xl">
      <%= render "form", category: @category %>
    </div>
    <div class="mt-4">
      <%= link_to "Cancel", @category, class: "ck-btn-outline" %>
    </div>
  <% end %>
</div>